PKG_CFLAGS = -I$(CURDIR)
PKG_LIBS = -L$(CURDIR) $(CURDIR)/serve.so
GO := $(shell which go)

.NOTPARALLEL: all

all: serve.so $(SHLIB)

serve.so: serve.go
	echo $(GO) && $(GO) build -o serve.so -buildmode=c-shared serve.go
